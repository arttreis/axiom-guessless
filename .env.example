# ============================================
# Axiom — Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e preencha com suas credenciais reais.
# NUNCA faça commit do arquivo .env com credenciais.
# ============================================

# --------------------------------------------
# Modo de operação
# --------------------------------------------
# true  = demo/apresentação (sem Supabase/Stripe — mocks ativos)
# false = produção (integrações reais ativas)
VITE_STATIC_MODE=false

# --------------------------------------------
# Supabase — Auth + Database
# --------------------------------------------
# Acesse: app.supabase.com → seu projeto → Settings → API
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# --------------------------------------------
# Anthropic — Claude API (geração de conteúdo)
# --------------------------------------------
# Acesse: console.anthropic.com → API Keys
# Se não configurado, o app roda em modo demo (mocks de IA)
VITE_ANTHROPIC_API_KEY=sk-ant-...

# --------------------------------------------
# URL da aplicação
# --------------------------------------------
VITE_APP_URL=http://localhost:5173

# ============================================
# AIOS Framework (não são variáveis do Axiom)
# ============================================

# DeepSeek API (para comando claude-free)
DEEPSEEK_API_KEY=

# OpenRouter API (para roteamento multi-modelo)
OPENROUTER_API_KEY=

# Anthropic API (sem prefix VITE_ — uso server-side/AIOS)
ANTHROPIC_API_KEY=

# OpenAI API Key
OPENAI_API_KEY=

# Exa Search API (busca web para agentes)
EXA_API_KEY=

# Supabase (sem prefix VITE_ — uso server-side/AIOS)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# GitHub Token (para GitHub CLI e acesso à API)
GITHUB_TOKEN=

# N8N (automação de workflows)
N8N_API_KEY=
N8N_WEBHOOK_URL=

# Sentry (error tracking)
SENTRY_DSN=

# Railway (deploy)
RAILWAY_TOKEN=

# Vercel (deploy)
VERCEL_TOKEN=

# AIOS Core
NODE_ENV=development
AIOS_VERSION=2.2.0
